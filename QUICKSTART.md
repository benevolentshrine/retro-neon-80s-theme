# 🚀 Quick Start Guide

Get up and running with **Retro Neon 80s** in seconds!

## ⚡ 3-Step Setup

### 1. Install
- Download the `.vsix` file from [Releases](../../releases)
- Open VS Code Extensions (`Ctrl+Shift+X`)
- Drag and drop the `.vsix` file into the Extensions view
- **OR** use command: `code --install-extension retro-neon-80s-1.0.0.vsix`

### 2. Activate
- Press `Ctrl+K` then `Ctrl+T` (or `Cmd+K` `Cmd+T` on Mac)
- Select **Retro Neon 80s**

### 3. Optimize
Add these to your `settings.json` for the full experience:

```json
{
  "editor.fontFamily": "'Fira Code', monospace",
  "editor.fontLigatures": true,
  "editor.cursorBlinking": "smooth",
  "workbench.colorTheme": "Retro Neon 80s"
}
```

## 🎮 Enjoy!
That's it! You're ready to code like it's 1985. 🌆✨
